[user]
	name = Robert Bohne
	email = robert.bohne@redhat.com
	signingkey = 9E8653F7D343455A
[push]
  default = simple
[core]
  excludesfile = /Users/rbo/.git/ignore_global
[diff]
  tool = vimdiff
[alias]
  l = log --oneline --decorate --graph --all
  bl = branch --list --all -vv
[init]
	defaultBranch = main
[credential "https://github.com"]
	helper =
	helper = !/usr/bin/gh auth git-credential


[pager]
   # diff = delta
    log = delta
    reflog = delta
    show = delta

#[interactive]
#    diffFilter = delta --color-only --features=interactive

[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
